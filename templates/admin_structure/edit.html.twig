{% extends 'base.html.twig' %}

{% block title %}Modifier la structure{% endblock %}

{% block body %}
    <h1 class="ms-3">Modifier la structure</h1>

<div class="container-fluid">

	{{ form_start(form) }}
	{{ form_widget(form) }}
	{{ form_end(form) }}
	<div class="d-flex justify-content-evenly">
	<button class="btn btn-primary">{{ button_label|default('Mettre à jour') }}</button>
	<a class="btn btn-info" href="{{ path('app_admin_franchise_index') }}">Retour</a>
	{{ include('admin_structure/_delete_form.html.twig') }}
	</div>
</div>

{% endblock %}
