{% extends 'base.html.twig' %}

{% block title %}Structure index{% endblock %}

{% block body %}
    <h1>Structure index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Sell_drink</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for structure in structures %}
            <tr>
                <td>{{ structure.name }}</td>
                <td>{{ structure.sellDrink ? 'Yes' : 'No' }}</td>
                <td>
                    <a href="{{ path('app_admin_structure_show', {'id': structure.id}) }}">show</a>
                    <a href="{{ path('app_admin_structure_edit', {'id': structure.id}) }}">edit</a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_admin_structure_new') }}">Create new</a>
{% endblock %}
